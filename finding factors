#include<stdio.h>
int main()
{
    int n;
    printf("enter a number");
    scanf("%d", &n);
    for(int i=1;i<n;i++)
    {
    if(n%i==0){
        printf("the factors are %d\n",i );}
    }
    return 0;
}
